# 🚗 ระบบจองที่จอดรถ (Parking Booking System)

## ✨ ฟีเจอร์หลัก

### 🎯 คอนเซ็ปต์: จองก่อน → รออนุมัติ → ได้ตั๋ว

1. **สำหรับผู้ใช้ทั่วไป:**

   - 📝 จองที่จอดรถ (กรอกข้อมูลรถ, วันเวลา)
   - ⏳ ระบบจะอยู่ในสถานะ `WAITING` (รออนุมัติ)
   - 📋 ดูรายการจองทั้งหมด
   - 🎫 ดาวน์โหลด/พิมพ์ตั๋วจอดรถ (หลังอนุมัติ)

2. **สำหรับ Admin:**

   - 👨‍💼 Dashboard แสดงรายการรออนุมัติ
   - ✅ อนุมัติการจอง (เปลี่ยนเป็น `APPROVED` + มอบหมายที่จอด + สร้างตั๋ว)
   - ❌ ปฏิเสธการจอง
   - 📊 ดูสถิติและรายงาน

3. **ระบบที่จอดรถ:**
   - 🗺️ แผนที่ที่จอดรถ 3 โซน (A, B, C)
   - 🟢 แสดงสถานะว่าง/เต็ม Real-time
   - 🅿️ จำนวนที่จอดทั้งหมด 30 ช่อง

## 🚀 วิธีติดตั้งและรัน

### 1. ติดตั้ง Dependencies

```bash
pip install django --break-system-packages
```

### 2. Run Migrations

```bash
python3 manage.py makemigrations
python3 manage.py migrate
```

### 3. สร้างข้อมูลตัวอย่าง

```bash
python3 setup_data.py
```

### 4. รันเซิร์ฟเวอร์

```bash
python3 manage.py runserver 0.0.0.0:8000
```

## ☁️ Deploy ไปยัง Render

โปรเจกต์นี้เตรียมไฟล์ `render.yaml` + `Procfile` ไว้ให้แล้ว คุณสามารถนำขึ้น Render ได้ทันทีด้วยขั้นตอนต่อไปนี้:

1. **เตรียมโค้ด** – Push โปรเจกต์ขึ้น GitHub/GitLab แล้วตรวจสอบว่า `render.yaml` อยู่ที่ root ของ repo
2. **สร้างบริการ** – ที่ Render เลือก `New > Blueprint` และเชื่อมกับ repo นี้ ระบบจะอ่านค่าจาก `render.yaml` ให้เอง
3. **ตั้งค่า Environment Variables**
   - `SECRET_KEY` – ใช้ค่าที่สร้างเองหรือให้ Render generate ให้อัตโนมัติ
   - `DEBUG=False`
   - `DATABASE_URL` – สร้าง PostgreSQL บน Render แล้ว copy ค่า `External Database URL`
   - (ออปชัน) `PRODUCTION_HOST` หากมีโดเมนเอง หรือ Render จะส่งค่าผ่าน `RENDER_EXTERNAL_HOSTNAME` ให้อัตโนมัติ
4. **Deploy** – Render จะรัน `pip install -r requirements.txt`, `collectstatic` และขณะ start จะ `migrate` ให้อัตโนมัติ จากนั้นเปิดแอปด้วย `gunicorn config.wsgi`

> ✅ ปลั๊ก Static Files ใช้ WhiteNoise แล้วเรียบร้อย จึงไม่ต้องตั้ง CDN เพิ่มก็เสิร์ฟไฟล์บน Render ได้ทันที

## 🔐 ข้อมูลเข้าสู่ระบบ

### Admin (จัดการระบบ)


- **URL:** `/admin/` หรือ `/admin-dashboard/`

### User ทั่วไป



## 📱 หน้าเว็บที่สำคัญ

| URL                 | หน้า            | คำอธิบาย                    |
| ------------------- | --------------- | --------------------------- |
| `/`                 | หน้าแรก         | แสดงแผนที่ที่จอดรถและสถิติ  |
| `/create/`          | จองที่จอดรถ     | ฟอร์มจองที่จอดรถ            |
| `/my-bookings/`     | การจองของฉัน    | รายการจองทั้งหมดของผู้ใช้   |
| `/booking/{id}/`    | รายละเอียด      | รายละเอียดการจอง            |
| `/ticket/{id}/`     | ตั๋วจอดรถ       | แสดงตั๋วพร้อม QR Code       |
| `/admin-dashboard/` | Admin Dashboard | สำหรับ admin อนุมัติ/ปฏิเสธ |

## 🎨 เทคโนโลยีที่ใช้

- **Backend:** Django 5.2
- **Frontend:** Tailwind CSS 3.0 (CDN)
- **Database:** SQLite (default)
- **Icons:** Emoji 🎉

## 📊 โครงสร้าง Database

### Models

1. **ParkingSpot** - ที่จอดรถ

   - `spot_number`: เลขที่จอด (เช่น A01, B05)
   - `zone`: โซน (A, B, C)
   - `is_available`: สถานะว่าง/ไม่ว่าง

2. **Booking** - การจอง

   - `booking_id`: รหัสจอง (auto-generate)
   - `status`: WAITING → APPROVED → REJECTED
   - `car_license`, `car_model`: ข้อมูลรถ
   - `booking_date`, `start_time`, `end_time`: วันเวลา
   - `parking_spot`: ที่จอดที่ได้รับ (หลังอนุมัติ)

3. **Ticket** - ตั๋วจอดรถ
   - `ticket_number`: เลขที่ตั๋ว (auto-generate)
   - `booking`: เชื่อมกับการจอง (OneToOne)
   - `qr_code`: QR Code สำหรับสแกน

## 🔄 Flow การทำงาน

```
ผู้ใช้จอง → Status: WAITING
            ↓
     Admin อนุมัติ
            ↓
  Status: APPROVED + ได้ที่จอด
            ↓
      สร้าง Ticket
            ↓
   ผู้ใช้ดาวน์โหลดตั๋ว
```

## 🎯 Features ที่น่าสนใจ

- ✅ **Responsive Design** - ใช้งานได้ทั้ง Desktop/Mobile
- ✅ **Real-time Status** - แสดงสถานะที่จอดแบบ real-time
- ✅ **Auto-generate ID** - สร้างรหัสจองและเลขตั๋วอัตโนมัติ
- ✅ **Beautiful UI** - ออกแบบด้วย Tailwind CSS
- ✅ **Print Ticket** - พิมพ์ตั๋วได้ทันที
- ✅ **Thai Language** - รองรับภาษาไทยเต็มรูปแบบ

## 📝 To-Do (ปรับปรุงเพิ่มเติม)

- [ ] เพิ่มระบบแจ้งเตือนทาง Email
- [ ] สร้าง QR Code จริงด้วย qrcode library
- [ ] เพิ่มระบบชำระเงิน
- [ ] Dashboard analytics สำหรับ admin
- [ ] API สำหรับ Mobile App

## 👨‍💻 Developer

สร้างด้วย ❤️ โดย Claude + Django + Tailwind CSS

---

## 🆘 วิธีใช้งานเบื้องต้น

### สำหรับผู้ใช้:

1. เข้าสู่ระบบด้วย 
2. คลิก "จองที่จอด" บน Navigation
3. กรอกข้อมูลรถและเวลาที่ต้องการ
4. รอ Admin อนุมัติ
5. เมื่ออนุมัติแล้ว ไปที่ "การจองของฉัน" → คลิก "ดูตั๋ว"

### สำหรับ Admin:

1. เข้าสู่ระบบด้วย admin/admin123
2. คลิก "Admin" บน Navigation
3. ดูรายการรออนุมัติ
4. คลิก "อนุมัติ" หรือ "ปฏิเสธ"
5. ระบบจะสร้างตั๋วอัตโนมัติ

---
